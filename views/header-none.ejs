<header>
  <img src="" alt="" />
  <div class="userBox">
    <% if(isUserSession) { %>
    <div class="userBox">
      <a href="/user/mypage/1" class="login">마이페이지</a>
      <a href="/logout" class="signin">로그아웃</a>
    </div>
    <% } %>
    <!-- Modal -->
    <div class="userBox">
      <div class="signin" data-bs-toggle="modal" data-bs-target="#firstModal">
        로그인
      </div>
      <div class="login" data-bs-toggle="modal" data-bs-target="#secondModal">
        회원가입
      </div>
    </div>
    <!-- Button trigger modal -->
    <div
      class="modal fade"
      id="firstModal"
      tabindex="-1"
      aria-labelledby="exampleModalLabel"
      aria-hidden="true"
    >
      <div class="modal-dialog">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title" id="exampleModalLabel">로그인</h5>
            <button
              type="button"
              class="btn-close"
              data-bs-dismiss="modal"
              aria-label="Close"
            ></button>
          </div>
          <div class="modal-body"><%- include("login.ejs") %></div>
          <div class="modal-footer">
            <div
              class="signin"
              data-bs-toggle="modal"
              data-bs-target="#secondModal"
            >
              회원가입
            </div>

            <button
              type="button"
              class="btn btn-secondary"
              data-bs-dismiss="modal"
            >
              닫기
            </button>
          </div>
          <!-- <div class="modal-footer"> -->
        </div>
      </div>
    </div>
  </div>

  <div
    class="modal fade"
    id="secondModal"
    tabindex="-1"
    aria-labelledby="exampleModalLabel"
    aria-hidden="true"
  >
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="exampleModalLabel">회원가입</h5>
          <button
            type="button"
            class="btn-close"
            data-bs-dismiss="modal"
            aria-label="Close"
          ></button>
        </div>
        <div class="modal-body">
          <!-- 회원가입  -->
          <%- include("signup.ejs") %>
        </div>
        <div class="modal-footer">
          <div
            class="signin"
            data-bs-toggle="modal"
            data-bs-target="#firstModal"
          >
            로그인
          </div>

          <button
            type="button"
            class="btn btn-secondary"
            data-bs-dismiss="modal"
          >
            닫기
          </button>
        </div>
      </div>
    </div>
  </div>
</header>
